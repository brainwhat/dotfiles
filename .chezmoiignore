# Repository instructions are source metadata, not a file in $HOME.
AGENTS.md

{{ if ne .chezmoi.os "darwin" }}
# These programs are only configured on macOS.
.aerospace.toml
.config/karabiner
{{ end }}
